# =============================================================================
# config.local.yaml — Machine-local configuration
# =============================================================================
# Copy this file to config.local.yaml and fill in your values.
# THIS FILE IS GITIGNORED — never commit config.local.yaml.
# Generated by: ./setup.sh (or fill manually)
# =============================================================================

# Absolute path to this kanbania root on your machine
# (auto-detected by config.sh via git rev-parse, but can be set explicitly)
# kanban_root: "/home/youruser/kanbania"

# =============================================================================
# Project paths (machine-specific)
# Each entry overrides or extends the matching entry in config.yaml > projects
# =============================================================================
projects:
  # my-project:
  #   path: "/home/youruser/Projects/my-project"
  #   worktrees:
  #     codex: "/home/youruser/Projects/my-project-codex"

# =============================================================================
# Notifications credentials (never commit these)
# Set notifications.provider in config.yaml, then fill credentials here
# =============================================================================
notifications:
  settings:
    # slack_webhook: "https://hooks.slack.com/services/..."
    # webhook_url: "https://your-server/webhook"
    # telegram_bot_token: "your-bot-token"
    # telegram_chat_id: "your-chat-id"
