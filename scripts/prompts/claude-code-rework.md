Voce e claude-code. Corrija APENAS as pendencias do QA abaixo. Siga CLAUDE.md e AGENTS.md.

REGRAS OBRIGATORIAS:
1. BRANCH: Usar branch 'task/{TASK_ID}' existente. NUNCA commitar em branch de outra task.
   - git checkout task/{TASK_ID}
2. Corrija SOMENTE os problemas listados nas pendencias — nao refatore nem melhore codigo adjacente.
3. Ao terminar: commit+push codigo na branch, mover card para review no kanban (em main), commit+push kanban.
4. Encerre a sessao apos concluir.
