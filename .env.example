# =============================================================================
# .env.example — Kanbania Environment Variables
# =============================================================================
# Copy this file to .env (root) and dashboard/.env.local (dashboard).
# Never commit files with real values — they are gitignored.
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Path to the kanbania root directory (where board/, sprints/, etc live)
# -----------------------------------------------------------------------------
KANBAN_ROOT=/home/youruser/kanbania

# -----------------------------------------------------------------------------
# REQUIRED: AI agent API keys — set only the ones you use
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-your-key-here
# OPENAI_API_KEY=sk-your-key-here
# GOOGLE_API_KEY=your-key-here

# -----------------------------------------------------------------------------
# AGENT IDENTITY: which agent is running in this process
# Matches an agent id from config.yaml > agents[].id
# Used by kb.sh and board-monitor.sh to know who is acting
# -----------------------------------------------------------------------------
KB_AGENT=claude-code

# -----------------------------------------------------------------------------
# DASHBOARD: port configuration
# -----------------------------------------------------------------------------
# Port for the Next.js dashboard
PORT=8765

# Port for the WebSocket server (file-watch events → real-time UI updates)
WS_PORT=8766

# WebSocket URL as seen by the browser client
# Leave unset to use automatic detection (same host, port WS_PORT)
# NEXT_PUBLIC_WS_URL=ws://your-server-hostname:8766

# -----------------------------------------------------------------------------
# NOTIFICATIONS (optional)
# Set the provider in config.yaml > notifications.provider
# Then fill the credentials here
# -----------------------------------------------------------------------------
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
# TELEGRAM_BOT_TOKEN=your-bot-token
# TELEGRAM_CHAT_ID=your-chat-id

# -----------------------------------------------------------------------------
# GIT (optional overrides — defaults come from config.yaml)
# -----------------------------------------------------------------------------
# GIT_AUTHOR_NAME="Kanbania Agent"
# GIT_AUTHOR_EMAIL="agent@kanbania"
